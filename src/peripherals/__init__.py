"""Real-world I/O bridges for the emulation core in `c64`/`c6502`.

Everything here is optional -- the emulation core has zero runtime
dependencies, and stays that way. Install this project's `peripherals`
extra (`pip install -e ".[peripherals]"`) to use anything in this
package. See docs/roadmap.md's Phase 8 onward.
"""
