"""Commodore 64 emulator -- the actual point of this repo.

Nothing implemented yet. See CLAUDE.md and docs/roadmap.md for the
phased plan (ROM licensing research first, then a real C64 Bus with
bank-switching, then CIA, then VIC-II text mode, then sprites/raster-IRQ
precision, then SID) and for what already exists to build on
(src/c6502/ -- a validated 6502 CPU core and assembler, ported from the
c-compiler-6502 repo, not something to redesign).
"""
